name: first_flow
on: push
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run:  echo "Hello, world!"
      - name: list files
        run: ls
      - name: read file
        run: cat README.md
